version = 'this is the project version'
description = 'this is the project description'
task showProjectProperties << {
    println version
    println project.description
}

ext.property1 = "this is property1"

ext {
    property2 = "this is property2"
}

task showProperties << {
    println property1
    println property2
}

//gradle -Pproperty3=xxx showCommandLineProperties1
task showCommandLineProperties1 << {
    println property3
}
//gradle -Dproperty3=xxx showCommandLineProperties2
task showCommandLineProperties2 << {
    String property = System.getProperty('property3')
    println property
}


class Foo {
    def name

    def myMethod() {
        prtintln "调用了myMethod方法"
    }
}

task judgeProperty {
    def foo = new Foo()
    println """judge an unexist property,page:: ${foo.metaClass.hasProperty(foo, 'age')}"""
    println """判定一个存在的属性name: ${foo.metaClass.hasProperty(foo, 'name')}"""
    println """判定一个存在的方法myMethod: ${foo.metaClass.respondsTo(foo, 'myMethod')}"""
    println """判定一个不存在的方法ask: ${foo.metaClass.respondsTo(foo, 'ask')}"""
}